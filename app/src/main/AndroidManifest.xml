<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.haythamayyash.jordanweather">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.haythamayyash.jordanweather.common.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.haythamayyash.jordanweather.presentation.weather_main.presenter.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.haythamayyash.jordanweather.common.BaseActivity" />
        <activity
            android:name="com.haythamayyash.jordanweather.presentation.weather_details.presenter.WeatherDetailsActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>

</manifest>